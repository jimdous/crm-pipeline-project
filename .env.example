DATABASE_URL=postgresql://localhost:5432/crm_pipeline
# Generate a long random key; never put it in browser source or commit it.
API_KEY=
# Public reads are only appropriate for the synthetic portfolio dataset.
PUBLIC_DEMO=false
CORS_ORIGINS=[]

